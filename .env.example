# Example environment variables for olinxra-backend
# DO NOT put real secrets here. Use your deployment secret manager.

# MongoDB
MONGO_URI=mongodb+srv://<user>:<password>@cluster.example.mongodb.net/?retryWrites=true&w=majority
MONGO_DB_NAME=olinxra

# Firebase admin credentials (JSON string). In production, prefer a secret manager.
# Example: set the full JSON as a single-line value or reference a file via FIREBASE_CRED_PATH
FIREBASE_CRED_JSON={"type":"service_account","project_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"..."}
# Alternatively, point to a file inside the container
# FIREBASE_CRED_PATH=/secrets/firebase-cred.json

# Google Cloud Storage
GCS_BUCKET_LOGOS=olinxra-logos
GCS_BUCKET_CONTEUDO=olinxra-conteudo
# Optionally provide the JSON for service account used by GCS
# CLOUD_STORAGE_CRED_JSON={"type":"service_account",...}
GOOGLE_APPLICATION_CREDENTIALS=/path/to/cloud-storage-cred.json

# App settings
SECRET_KEY=replace_with_random_secret
USER_ADMIN_EMAIL=admin@example.com
APP_PASSWORD=replace_with_app_password

# CORS: comma-separated list of allowed origins
CORS_ALLOW_ORIGINS=https://your-frontend.example.com,http://localhost:5173

# GLB generator settings
GLB_ALLOWED_DOMAINS=storage.googleapis.com,firebasestorage.googleapis.com
GLB_MAX_IMAGE_BYTES=5000000
GLB_MAX_DIM=2048

# Optional: Atlas/CI keys (keep in secret manager)
ATLAS_PUBLIC_KEY=
ATLAS_PRIVATE_KEY=
ATLAS_PROJECT_ID=

# Environment
ENV=production
